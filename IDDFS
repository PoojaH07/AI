Algorithm IDDFS(start, goal):

1. Set depth_limit = 0

2. Repeat:
      a. result = Depth_Limited_DFS(start, goal, depth_limit)
      b. If result = FOUND
             return "Goal Found!"
      c. Else
             Increase depth_limit by 1

3. Until goal is found or depth_limit reaches max allowed



Function Depth_Limited_DFS(node, goal, limit):

1. If node == goal:
        return FOUND

2. If limit == 0:
        return NOT_FOUND

3. For each child in node's neighbors:
        result = Depth_Limited_DFS(child, goal, limit - 1)
        If result == FOUND:
             return FOUND

4. Return NOT_FOUND
